"""Closed forms, bundled byte-identically from the project's own `truth/`.

Checked by `tests/test_environment.py::test_the_bundled_truth_has_not_drifted`.
Never import `src/` from here — spec §6.4 rule 4: the value a gate is checked
against cannot be produced by the code under test.
"""
