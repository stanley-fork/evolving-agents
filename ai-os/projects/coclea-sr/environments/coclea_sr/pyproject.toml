[project]
name = "coclea-sr"
version = "0.1.0"
description = "coclea-sr — physics-gated tasks: eigenvalues, Sturm-Liouville, Lyapunov. Verified against closed forms, not a compiler."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["coclea_sr"]
