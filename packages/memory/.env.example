# LLM Provider API Keys (optional — only needed for real dream cycles)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Embedding model (default: all-MiniLM-L6-v2)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# SQLite database path
CTE_DB_PATH=memory.db

# FAISS index path
CTE_FAISS_PATH=memory.faiss
